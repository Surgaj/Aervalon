name: Diagnose Aervalon Runtime

on:
  push:
    branches: [diagnostic-runtime]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout diagnostic branch
        uses: actions/checkout@v4

      - name: Install Godot and export templates
        uses: firebelley/godot-export@v6.0.0
        with:
          godot_executable_download_url: https://github.com/godotengine/godot/releases/download/4.5.1-stable/Godot_v4.5.1-stable_linux.x86_64.zip
          godot_export_templates_download_url: https://github.com/godotengine/godot/releases/download/4.5.1-stable/Godot_v4.5.1-stable_export_templates.tpz
          relative_project_path: ./
          archive_output: false

      - name: Run source project headless
        shell: bash
        run: |
          GODOT="$HOME/.local/share/godot/godot_executable/Godot_v4.5.1-stable_linux.x86_64"
          set +e
          timeout 20s "$GODOT" --headless --path . --verbose --quit-after 10 2>&1 | tee runtime.log
          SRC_RC=${PIPESTATUS[0]}
          echo "SOURCE_RUNTIME_EXIT=$SRC_RC"
          grep -E "ERROR|SCRIPT ERROR|Parse Error|Invalid|Failed loading|WARNING" runtime.log || true
          exit 0

      - name: Run exported Web PCK headless
        shell: bash
        run: |
          GODOT="$HOME/.local/share/godot/godot_executable/Godot_v4.5.1-stable_linux.x86_64"
          PACK="$HOME/.local/share/godot/builds/Web/index.pck"
          set +e
          timeout 20s "$GODOT" --headless --main-pack "$PACK" --verbose --quit-after 10 2>&1 | tee pck-runtime.log
          PCK_RC=${PIPESTATUS[0]}
          echo "PCK_RUNTIME_EXIT=$PCK_RC"
          grep -E "ERROR|SCRIPT ERROR|Parse Error|Invalid|Failed loading|WARNING" pck-runtime.log || true
          exit 0

      - name: Browser smoke test with Chromium
        shell: bash
        run: |
          WEB="$HOME/.local/share/godot/builds/Web"
          python3 -m http.server 8765 --directory "$WEB" > /tmp/aervalon-http.log 2>&1 &
          SERVER_PID=$!
          sleep 2
          set +e
          google-chrome --headless --no-sandbox --disable-dev-shm-usage --enable-logging=stderr --v=1 --use-angle=swiftshader --enable-unsafe-swiftshader --virtual-time-budget=10000 --screenshot=/tmp/aervalon-chrome.png http://127.0.0.1:8765/ > /tmp/chrome.out 2> /tmp/chrome.err
          CHROME_RC=$?
          echo "CHROME_EXIT=$CHROME_RC"
          grep -E "CONSOLE|ERROR|error|WebGL|wasm|Godot|exception|failed" /tmp/chrome.err | tail -200 || true
          file /tmp/aervalon-chrome.png || true
          ls -lh /tmp/aervalon-chrome.png || true
          kill $SERVER_PID || true
          exit 0
